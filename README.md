Update types autocomplete

php artisan ide-helper:generate
php artisan ide-helper:models -M

or just 
php artisan app:update-autocomplete

use this commands to get your UID & GID
id -u
id -g

set it in .env to UID & GID (dev only)
